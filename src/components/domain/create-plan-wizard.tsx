'use client'

// Temporary stub — replace with full wizard implementation
export function CreatePlanWizard(props: {
  open: boolean
  onClose: () => void
  siteId: string
  onCreated: (id: string) => void
}) {
  if (!props.open) return null
  return null
}
